apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # O nome 'db-service' será o endereço do banco dentro do Kubernetes
  DATABASE_URL: "postgres://admin@db-service:5432/pedidos_db"
  NODE_ENV: "production"